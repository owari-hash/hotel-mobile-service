import OrderClientWrapper from './order-client-wrapper'; // New import

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Захиалгын хуудас',
};

export default function OrderPage() {
  // This is the Order Page
  return <OrderClientWrapper />;
}
